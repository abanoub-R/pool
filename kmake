#!/bin/sh

cd /usr/src/linux

make clean
make -j8
make -j8 modules_install
make install

# clean up
cd /boot
rm *.old

# update grub
grub-mkconfig -o /boot/grub/grub.cfg

echo "Reboot? (y/n)"
read choice

case $choice in
	y)
	  echo "Rebooting..."
	  reboot
	  ;;
	
	*)
	  echo "Quitting"
esac
